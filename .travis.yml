language: node_js

node_js:
  - "8"

cache:
  directories:
    - "node_modules"

install:
- npm install -g angular-cli
- npm install -g karma
- npm install
- ng build
- npm test

script:
- ng lint

notifications:
  email: false
